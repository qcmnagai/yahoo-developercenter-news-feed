{
    "require": {
        "fabpot/goutte": "^3.2",
        "mibe/feedwriter": "^1.1"
    },
    "scripts": {
        "build": [
            "php ./builder.php"
        ],
        "publish": [
            "git diff --exit-code || git commit -am 'Update feed by ci' && git push origin master"
        ]
    }
}
